# Dagster Orchestrator

## Usage
1. Install [Pixi](https://pixi.prefix.dev/)
2. Setup the Python environment with: `cd app/core && pixi shell && pixi run postinstall`
3. Create a `app/core/.env` like  `app/core/.env.example`
4. Start the Dagster orchestrator in dev-mode with `dagster dev`
